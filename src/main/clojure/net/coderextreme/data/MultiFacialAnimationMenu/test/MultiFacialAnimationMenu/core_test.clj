(ns MultiFacialAnimationMenu.core-test
  (:require [clojure.test :refer :all]
            [MultiFacialAnimationMenu.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
