(ns DesignPatternsApparelMedicalSkinLayers.core-test
  (:require [clojure.test :refer :all]
            [DesignPatternsApparelMedicalSkinLayers.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
